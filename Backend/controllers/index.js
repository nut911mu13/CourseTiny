const auth = require('./auth');
const course = require('./course');
const user = require('./user');
const category = require('./category');
const order = require('./order');
const form = require('./form');

module.exports = {
  auth, course, user, category, order, form,
};
